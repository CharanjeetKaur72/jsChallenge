// **** Activity 1 : Function Declaration ***********

// Activity 1: Task 2: Calculate square of a number and return the result

function calcSquare(num = 1) {
  num = Math.round(num);
  return Math.pow(num, 2);
  // return num ** 2;       // other method to get square of a number
}
